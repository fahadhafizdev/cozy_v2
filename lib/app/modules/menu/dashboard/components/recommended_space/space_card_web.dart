import 'package:flutter/material.dart';

class SpaceCardWeb extends StatelessWidget {
  const SpaceCardWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
